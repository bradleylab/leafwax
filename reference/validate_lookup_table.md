# Validate lookup table

Checks that a lookup table is valid and contains all required
components.

## Usage

``` r
validate_lookup_table(lookup_table)
```

## Arguments

- lookup_table:

  Object to validate

## Value

Logical TRUE if valid, otherwise throws an error
