# Monitor memory usage during batch processing

Utility function to track memory usage during large batch operations.

## Usage

``` r
monitor_memory(message = NULL)
```

## Arguments

- message:

  Optional message to print with memory info

## Value

List with memory statistics
