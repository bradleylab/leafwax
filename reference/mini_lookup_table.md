# Mini lookup table

Compact precomputed lookup for fast non-Bayesian inversion smoke tests.
Used by examples in
[`create_lookup_table`](https://bradleylab.github.io/leafwax/reference/create_lookup_table.md).

## Usage

``` r
mini_lookup_table
```

## Format

A data frame with one row per grid cell.
