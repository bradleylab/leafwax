# Standardize a coord matrix using the scaling parameters.

Standardize a coord matrix using the scaling parameters.

## Usage

``` r
standardize_coords(coords, scaling)
```

## Arguments

- coords:

  matrix(n, 2) of (lon, lat) in degrees

- scaling:

  list with \$lon_mean, \$lon_sd, \$lat_mean, \$lat_sd
