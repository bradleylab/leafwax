# Get all model metadata

Returns a list of all 14 models with their descriptions and properties.
The models are based on the spatial leafwax hierarchical Bayesian
models.

## Usage

``` r
get_all_model_metadata()
```

## Value

Named list of available models with metadata
