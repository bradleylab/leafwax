# Get model parameters

Returns which parameters each model expects and provides metadata about
model capabilities for validation.

## Usage

``` r
get_model_parameters(model_name)
```

## Arguments

- model_name:

  Name of the model

## Value

List with model parameters and capabilities
