% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_sigma_within.R
\name{estimate_sigma_within}
\alias{estimate_sigma_within}
\title{Estimate the within-record residual SD of a leaf-wax record}
\usage{
estimate_sigma_within(
  d2h_wax,
  age,
  baseline_interval = NULL,
  detrend = c("none", "linear", "loess"),
  ar1_correction = TRUE
)
}
\arguments{
\item{d2h_wax}{Numeric vector of leaf-wax delta-2-H measurements
(per mil).}

\item{age}{Numeric vector of sample ages (any monotone time-like
variable; same length as \code{d2h_wax}).}

\item{baseline_interval}{Length-2 numeric \code{c(min, max)} defining the
baseline window in \code{age} units. \code{NULL} means use the full record
(warning emitted).}

\item{detrend}{One of \code{"none"} (default), \code{"linear"}, or \code{"loess"},
describing how to remove trends within the baseline before
computing residuals. \code{"linear"} fits \code{lm(d2h_wax ~ age)}.
\code{"loess"} fits \code{loess(d2h_wax ~ age, span = 0.75)}.}

\item{ar1_correction}{Logical (default \code{TRUE}); if \code{TRUE}, applies
the AR(1) variance reduction described above.}
}
\value{
A list with elements:
\itemize{
\item \code{sigma_within} - point estimate (per mil), AR(1)-corrected
if requested.
\item \code{sigma_within_se} - asymptotic standard error of the
returned \code{sigma_within}.
\item \code{sigma_naive} - sample SD of residuals before AR(1)
correction.
\item \code{n_baseline} - number of samples used.
\item \code{rho_t_baseline} - lag-1 temporal autocorrelation of
residuals in the baseline.
\item \code{method} - one-line description of the choices made.
}
}
\description{
Estimate a within-record residual standard deviation \code{sigma_within}
from a stratigraphic baseline interval of a downcore leaf-wax
delta-2-H record. Within-record residual variance is generally smaller
than the global posterior sigma, because spatial structure cancels and
between-site sources of variance (laboratory, vegetation background,
basin size, integration timescale) do not vary inside a single record.
Manuscript Section 4.5.3 explains why and frames the obligations on
the user.
}
\details{
The function operates on the baseline interval only. With
\code{baseline_interval = NULL}, the entire record is treated as the
baseline and a warning is emitted: this conflates real climate
variability with measurement and process noise, so the returned
\code{sigma_within} is an upper bound rather than a defended estimate.
Real use should specify a stratigraphic interval over which
stationarity of vegetation, hydrology, and source-water seasonality
can be defended on independent grounds.

Within the baseline window, the function:
\enumerate{
\item subsets the record to the baseline,
\item optionally detrends (\code{"linear"} or \code{"loess"}) to remove
long-wavelength trends that the user does not want absorbed
into the residual SD,
\item computes the lag-1 temporal autocorrelation (\code{rho_t})
of the residuals,
\item returns the naive standard deviation of the residuals
(\code{sd(residuals)}) and an AR(1)-corrected effective SD when
\code{ar1_correction = TRUE}. The correction is
\code{sigma_eff = sigma_naive * sqrt(1 - rho_t^2)} for \verb{|rho_t| < 1},
a conservative reduction that accounts for the variance
sequential samples share.
}

The standard error of \code{sigma_within} is approximated by
\code{sigma_within / sqrt(2 * (n_baseline - 1))}, the asymptotic SE of
a normal-distribution sample SD.
}
\examples{
\donttest{
# Synthetic stationary baseline: noise around a flat mean
set.seed(1)
n  <- 80
ag <- seq(0, 8000, length.out = n)
d  <- -160 + rnorm(n, 0, 5)
est <- estimate_sigma_within(d, ag,
                             baseline_interval = c(0, 4000),
                             detrend = "none",
                             ar1_correction = TRUE)
est$sigma_within
}
}
