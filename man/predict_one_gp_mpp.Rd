% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_interpolation.R
\name{predict_one_gp_mpp}
\alias{predict_one_gp_mpp}
\title{Predict an mPP Gaussian-process random effect at a new location}
\usage{
predict_one_gp_mpp(
  coords_new,
  knot_coords,
  z_knots,
  sigma_draws,
  ls_km_draws,
  scaling,
  jitter = 1e-04
)
}
\arguments{
\item{coords_new}{matrix(n_obs, 2) of (lon, lat) in DEGREES.}

\item{knot_coords}{matrix(n_knots, 2) of (lon, lat) in DEGREES.}

\item{z_knots}{matrix(n_draws, n_knots) of standardized knot effects
(e.g. \verb{z_intercept_spatial[1..125]} from the posterior).}

\item{sigma_draws}{numeric(n_draws), the GP marginal SD.}

\item{ls_km_draws}{numeric(n_draws), the GP length scale in km
(e.g. \code{ls_intercept_km}).}

\item{scaling}{list with \code{lon_mean}, \code{lon_sd}, \code{lat_mean}, \code{lat_sd}.}

\item{jitter}{ridge added to K_knots for numerical stability.}
}
\value{
matrix(n_draws, n_obs) of predicted GP values at the new sites.
}
\description{
Single-GP version. Used internally by \code{predict_spatial_dual_gp()} for
each of the two (intercept, slope) fields. Matches the Matern 3/2 kernel
and standardized-coordinate convention from the v10 Stan model.
}
\keyword{internal}
