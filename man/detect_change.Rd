% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_change.R
\name{detect_change}
\alias{detect_change}
\title{Within-record d2H_precip change detection}
\usage{
detect_change(
  reconstruction,
  age,
  baseline_interval,
  test_intervals = NULL,
  sigma_within,
  sigma_analytical = 3,
  rho_t = NULL,
  beta_eff,
  confidence = 0.95,
  magnitudes = NULL
)
}
\arguments{
\item{reconstruction}{Output of \code{invert_d2H(..., return_full = TRUE, uncertainty_mode = "within_record", sigma_within = ...)} on a
downcore series. Must contain a \code{posterior_draws} matrix of shape
\verb{n_iter x n_samples}. The reconstruction must be built in
within-record mode with a positive \code{sigma_within}: the
change-detection threshold formula and \code{p_exceed} are derived
under the within-record substitution where \code{sigma_within}
replaces the global residual SD (manuscript Section 4.5.3).
Passing an absolute-mode reconstruction, or one without a
recorded \code{sigma_within}, raises an error.}

\item{age}{Numeric vector, length \code{n_samples}, of sample ages
matching the reconstruction columns.}

\item{baseline_interval}{Length-2 numeric \code{c(min, max)} defining the
baseline window in \code{age} units.}

\item{test_intervals}{Either a length-2 numeric vector for a single
test window, or a named list of length-2 numerics for multiple
windows. NULL skips the per-interval probability table and returns
only the threshold.}

\item{sigma_within}{Numeric, required, the within-record residual SD
in leaf-wax per mil (typically from \code{estimate_sigma_within()}).}

\item{sigma_analytical}{Numeric, the analytical uncertainty on
\code{d2H_wax} measurements in per mil (default 3).}

\item{rho_t}{Numeric, lag-1 temporal autocorrelation. Use
\code{estimate_temporal_autocorrelation()} to compute. Defaults to 0
(independent samples) with a message.}

\item{beta_eff}{Numeric, the local effective slope. Use
\code{local_effective_slope()} for a point estimate (e.g., its median).}

\item{confidence}{Numeric in (0, 1), the confidence level for the
detection threshold. Default 0.95.}

\item{magnitudes}{Optional numeric vector of magnitudes (per mil) to
evaluate posterior \verb{Pr(|delta| > magnitude)} against.}
}
\value{
A list with elements:
\itemize{
\item \code{threshold} - the detection threshold on \code{d2H_precip} at
the requested confidence level.
\item \code{formula} - the components used: \code{z}, \code{rho_t},
\code{sigma_within}, \code{sigma_analytical}, \code{beta_eff}.
\item \code{intervals} - a data frame with one row per test interval
reporting the posterior median and CI of \code{delta} and (if
\code{magnitudes} supplied) the posterior probability of exceeding
each magnitude.
}
}
\description{
Given a downcore \code{invert_d2H()} reconstruction posterior with full
draws (\code{return_full = TRUE}), report (a) the posterior probability
that the difference in mean \code{d2H_precip} between two stratigraphic
intervals exceeds user-supplied magnitudes, and (b) the within-
record 95\\% (or other) detection threshold from manuscript Section
4.5.3:
}
\details{
\deqn{\mathrm{threshold}_{precip} =
      \frac{z_{\alpha/2}\, \sqrt{2(1 - \rho_t)}\,
            \sqrt{\sigma_{within}^2 + \sigma_{analytical}^2}}
           {\beta_{\mathrm{eff}}}}

The threshold is the smallest difference in \code{d2H_precip} between two
independent samples that can be distinguished from within-record
noise at the chosen confidence level.
}
