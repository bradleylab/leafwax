% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assess_claim.R
\name{assess_claim}
\alias{assess_claim}
\title{Assess a paleoclimate claim against the leaf-wax taxonomy}
\usage{
assess_claim(
  record,
  claim,
  reconstruction = NULL,
  longitude = NULL,
  latitude = NULL,
  model_name = "baseline_sp",
  ...
)
}
\arguments{
\item{record}{Data frame (or list) with at least \code{d2h_wax} and \code{age}
columns of equal length. \code{d2h_wax_err} is optional; defaults to
\code{claim$sigma_analytical} per row.}

\item{claim}{Named list specifying the claim. Required fields:
\code{level} (integer 1-4, the level the user is asserting),
\code{interval_baseline} (length-2 numeric c(min, max) age window),
\code{interval_test} (length-2 numeric age window),
\code{sigma_within} (per mil; from \code{estimate_sigma_within()}).
Optional fields, used by higher levels:
\code{sigma_analytical} (default 3),
\code{rho_t} (default 0; from \code{estimate_temporal_autocorrelation()}),
\code{beta_eff} (numeric scalar; required at Level 3+),
\code{confidence} (default 0.95),
\code{magnitude_precip} (numeric, the precip-space magnitude the user
asserts; required at Level 3+),
\code{corroborating_proxies} (list, used at Level 2; the test is
non-empty + named),
\code{vegetation_stationary}, \code{seasonal_source_stationary},
\code{evapotranspirative_stationary} (each a list with \code{value} (TRUE)
and a non-empty \code{evidence} string; required at Level 4).}

\item{reconstruction}{Optional output of \code{invert_d2H(..., return_full = TRUE)} on the record. When NULL and the claim's level is 3 or 4,
the function runs the inversion itself given \code{longitude} /
\code{latitude} / \code{model_name}.}

\item{longitude, latitude}{Site coordinates, used only when
\code{reconstruction} is NULL.}

\item{model_name}{Model to use when running the inversion (default
"baseline_sp").}

\item{...}{Additional args forwarded to \code{invert_d2H()} (e.g.,
elevation, c4_fraction, pft_*, n_posterior_draws).}
}
\value{
A list with elements:
\itemize{
\item \code{highest_level} - integer in 0:4. 0 means even Level 1
did not clear.
\item \code{levels} - data frame, one row per level, with columns
\code{level}, \code{passed} (logical), \code{summary} (one-line reason).
\item \code{details} - per-level lists of computed quantities
(e.g., delta_wax, threshold, p_exceed, missing fields).
\item \code{claim} - the (validated) claim object.
}
}
\description{
Walks the four-level taxonomy from manuscript Section 4.5.6 and
reports the highest level a claim survives at. The taxonomy is:
\itemize{
\item Level 1: a leaf-wax delta-2-H change occurred between two
intervals. Defensible when the change exceeds analytical
uncertainty and the within-record residual SD \code{sigma_within}.
\item Level 2: the wax change is consistent with a directional
hydroclimate change. Requires corroborating evidence (multi-
proxy concordance, sedimentological context, or biomarker
evidence for vegetation stability) supplied via
\code{corroborating_proxies}.
\item Level 3: the wax change implies a quantitative
delta-2-H_precip magnitude. Requires a defended local effective
slope, a defended \code{sigma_within}, and explicit uncertainty
propagation through the inversion. When \code{reconstruction} is
NULL the function calls \code{invert_d2H()} itself.
\item Level 4: the magnitude is uniquely attributable to
precipitation isotope change rather than to vegetation,
source-water seasonality, or evapotranspirative enrichment.
Requires independent stationarity evidence for each non-
precipitation control over the interval.
}
}
\details{
Use this when a colleague claims that a downcore record shows a
specific d2H_precip shift and you want a structured check of which
levels of the taxonomy that claim actually clears.
}
