% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_change.R
\name{estimate_temporal_autocorrelation}
\alias{estimate_temporal_autocorrelation}
\title{Estimate lag-1 temporal autocorrelation}
\usage{
estimate_temporal_autocorrelation(
  d2h_wax,
  age,
  method = c("ar1", "lomb_scargle")
)
}
\arguments{
\item{d2h_wax}{Numeric vector of leaf-wax delta-2-H measurements
(per mil).}

\item{age}{Numeric vector of sample ages, same length as \code{d2h_wax}.}

\item{method}{One of \code{"ar1"} or \code{"lomb_scargle"}.}
}
\value{
Numeric scalar in \verb{[-1, 1]}, or \code{NA_real_} when the residuals
are constant (e.g., n < 3 finite samples).
}
\description{
Estimate the lag-1 autocorrelation \code{rho_t} of a leaf-wax record's
residuals after a flat-mean detrend, ordering by age. This is the
quantity that enters the within-record detection threshold from
manuscript Section 4.5.3 (\verb{Var(X1 - X2) = 2 sigma^2 (1 - rho_t)}).
}
\details{
Two methods are supported:
\itemize{
\item \code{"ar1"} (default): Pearson correlation of \code{resid[-n]} with
\code{resid[-1]} after age-ordering. For irregularly sampled series
this is an approximation; see \code{"lomb_scargle"} for an
alternative.
\item \code{"lomb_scargle"}: not yet implemented. Returns an error
pointing the user at \code{"ar1"} until the spectral implementation
lands. The plan is to estimate \code{rho_t} from the dominant
timescale of a Lomb-Scargle periodogram on the irregularly
sampled series.
}
}
\examples{
\donttest{
set.seed(1)
n <- 200
rho <- 0.7
e  <- numeric(n); e[1] <- rnorm(1, 0, 5)
for (k in 2:n) e[k] <- rho * e[k-1] + rnorm(1, 0, 5 * sqrt(1 - rho^2))
ag <- seq(0, 10000, length.out = n)
estimate_temporal_autocorrelation(-150 + e, ag)
}
}
