% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_data.R
\name{download_model_data}
\alias{download_model_data}
\title{Download model data from the configured public release}
\usage{
download_model_data(
  model_name,
  version = "latest",
  data_type = c("posteriors"),
  cache_dir = NULL,
  overwrite = FALSE,
  verify = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{model_name}{Character string specifying the model name}

\item{version}{Version tag to download (default "latest")}

\item{data_type}{Type of data to download (only "posteriors" is currently supported)}

\item{cache_dir}{Directory to save files (default uses get_cache_dir())}

\item{overwrite}{Logical whether to overwrite existing files}

\item{verify}{Logical whether to verify file integrity with checksums}

\item{verbose}{Logical whether to show progress messages}
}
\value{
Logical indicating success
}
\description{
Downloads model posterior draws from the release configured in
\code{inst/extdata/data_urls.json}. Downloads are accepted only when their byte
size and SHA-256 checksum match the configured release manifest.
}
\examples{
\dontrun{
cache_dir <- file.path(tempdir(), "leafwax_download_example")
ok <- download_model_data(
  "baseline",
  version = "latest",
  cache_dir = cache_dir,
  verify = TRUE,
  verbose = FALSE
)
\dontshow{unlink(cache_dir, recursive = TRUE, force = TRUE)}
}
}
