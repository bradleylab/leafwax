% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_vegetation_envelope.R
\name{compute_vegetation_envelope}
\alias{compute_vegetation_envelope}
\title{Vegetation-only envelope for a paleo wax-isotope record}
\usage{
compute_vegetation_envelope(
  oipc_ref,
  from,
  to,
  model_name = "full_interact_sp",
  n_draws = NULL,
  verbose = TRUE
)
}
\arguments{
\item{oipc_ref}{Numeric scalar, the calibration-period \code{d2H_precip}
at the site (per mil). Typically extracted from the OIPC raster
(Bowen and Wilkinson 2002) at the site coordinates; the package
does not bundle the raster. Held constant across the envelope by
construction.}

\item{from}{Named numeric vector of fractional PFT cover at the
baseline interval. Names must be exactly \code{tree}, \code{shrub}, \code{grass},
\code{C4} (case-sensitive). Values in [0, 1] with
\code{tree + shrub + grass <= 1}; \code{C4} is an independent fraction.}

\item{to}{Named numeric vector of fractional PFT cover at the test
interval. Same name and value constraints as \code{from}.}

\item{model_name}{Character, name of the calibration model to use.
Must contain all eight PFT coefficients
(\verb{beta_\{tree,shrub,grass,c4\}} and
\verb{beta_oipc_x_\{tree,shrub,grass,c4\}}). Default
\code{"full_interact_sp"} is the only shipped model that satisfies
this requirement.}

\item{n_draws}{Integer, number of posterior draws to use. \code{NULL}
(default) uses all available draws. Subsampling is deterministic
(stratified, via \code{\link[=load_posteriors]{load_posteriors()}}).}

\item{verbose}{Logical, whether to emit progress messages.}
}
\value{
List with
\itemize{
\item \code{envelope_draws} - numeric vector of per-draw signed
envelopes (per mil).
\item \code{envelope_median} - posterior median of \code{envelope_draws}.
\item \code{envelope_p975_abs} -
\code{quantile(abs(envelope_draws), 0.975)}, the absolute upper
bound used by \code{\link[=assess_claim]{assess_claim()}} path (b).
\item \code{oipc_ref} - the \code{oipc_ref} value used (echoed for
traceability).
\item \code{delta_pft} - named numeric vector \code{to - from}.
\item \code{n_draws_used} - integer, number of draws contributing to
\code{envelope_draws}.
\item \code{model_name} - the model name used.
\item \code{details} - list with \code{coefs_summary} (posterior median
of each of the eight coefficients).
}
}
\description{
Computes the posterior-propagated wax-shift envelope expected under
a user-supplied PFT-change scenario, holding \code{d2H_precip} constant.
This is the magnitude path of the Level 2 claim taxonomy described
in the accompanying manuscript Section 4.5.3: the calibration's PFT
main-effect and PFT-by-OIPC interaction coefficients are combined
across all posterior draws to bound how much wax change vegetation
reorganization alone can produce at the site, with no contribution
from precipitation-isotope change.
}
\details{
For each posterior draw the per-draw envelope is
\verb{envelope = sum_k beta_k * delta_pft_k + sum_k beta_oipc_x_k * oipc_ref * delta_pft_k}
where \code{k} indexes the four PFT classes (\code{tree}, \code{shrub}, \code{grass},
\code{C4}) and \code{delta_pft_k = to[k] - from[k]}. The returned
\code{envelope_p975_abs = quantile(abs(envelope_draws), 0.975)} is the
absolute upper bound used by \code{\link[=assess_claim]{assess_claim()}} path (b) to test
whether an observed \verb{|delta_wax|} exceeds what the supplied PFT
scenario alone can produce.

Passing path (b) rejects the vegetation-only null for the supplied
scenario at the site. It does not identify the hydroclimate
mechanism, quantify the precipitation-isotope change, or address
sediment-source change, depositional artifact, compound-source
mixing, age-model errors, evapotranspirative regime change, or
seasonality shifts (which \code{assess_claim()} gates with separate
fields). The calibration coefficients are derived from spatial
variation across sites; applying them to within-record temporal
vegetation change assumes the same response holds through time at
one location.
}
\section{Manuscript reference}{

Section 4.5.3 of the accompanying manuscript defines the
vegetation-only envelope and the constant-precipitation framing.
Section 4.5.6 places it in the Level 2 claim taxonomy.
}

\examples{
\donttest{
# Hypothetical: a 30 percentage-point woody-to-grass transition at a
# site where the OIPC raster lookup gave d2H_precip approximately -60 per mil.
env <- compute_vegetation_envelope(
  oipc_ref = -60,
  from = c(tree = 0.4, shrub = 0.3, grass = 0.2, C4 = 0.05),
  to   = c(tree = 0.1, shrub = 0.2, grass = 0.5, C4 = 0.20),
  model_name = "full_interact_sp",
  n_draws = 100,
  verbose = FALSE
)
env$envelope_p975_abs   # absolute 97.5\% upper bound of vegetation-only |Delta wax|
}
}
