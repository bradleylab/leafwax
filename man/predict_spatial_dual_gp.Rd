% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_interpolation.R
\name{predict_spatial_dual_gp}
\alias{predict_spatial_dual_gp}
\title{Predict both spatial intercept and spatial slope at new locations}
\usage{
predict_spatial_dual_gp(coords_new, knot_coords, draws, scaling)
}
\arguments{
\item{coords_new}{matrix(n_obs, 2) of (lon, lat) in DEGREES.}

\item{knot_coords}{matrix(n_knots, 2) of (lon, lat) in DEGREES.}

\item{draws}{data.frame of posterior draws (subset of
leafwax_posterior$draws). Must contain columns
\verb{z_intercept_spatial[1..n_knots]}, \verb{z_slope_spatial[1..n_knots]},
\code{sigma_intercept_spatial}, \code{sigma_slope_spatial},
and one of \code{ls_intercept_km} / \code{ls_slope_km}.}

\item{scaling}{list with \code{lon_mean}, \code{lon_sd}, \code{lat_mean}, \code{lat_sd}.}
}
\value{
list with two matrices, each n_draws x n_obs:
\code{intercept} (additive contribution to beta_0 in standardized
d2H_wax space) and \code{slope} (additive contribution to beta_oipc).
}
\description{
v10 carries two independent GPs. Both share knot coordinates and a
single length scale parameter (\code{ls_intercept_km == ls_slope_km} in
v10's posterior, two names for the same draw), but have distinct
\code{sigma_intercept_spatial} and \code{sigma_slope_spatial}, and distinct
\verb{z_intercept_spatial[*]} and \verb{z_slope_spatial[*]} knot effects.
}
